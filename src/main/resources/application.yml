server:
  port: 8080
  servlet:
    context-path: /
spring:
  config:
    import: optional:file:./.env
  application:
    name: pbms
  datasource:
    url: ${DB_URL:jdbc:sqlserver://localhost:1433;databaseName=pbms_db;encrypt=true;trustServerCertificate=true}
    username: ${DB_USERNAME:sa}
    password: ${DB_PASSWORD:12345}
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    database-platform: org.hibernate.dialect.SQLServerDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: dbo
    show-sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true
    validate-on-migrate: false
    locations: classpath:db/migration


jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  expiration: 86400000

google:
  client:
    id: ${GOOGLE_CLIENT_ID:643517877685-3s06vf1g291nld9730fv5fun270mbneb.apps.googleusercontent.com}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3001}

iot:
  api:
    key: PBMS-HARDWARE-SECURE-KEY-2024
